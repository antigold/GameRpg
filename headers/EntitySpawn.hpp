#include <random>
#include <vector>
#include "Entity.hpp"


std::vector<int> generateRandomPosition(int boardSize,Player* player);
std::vector<int> generateRandomItemPosition(int boardSize, Player* player);